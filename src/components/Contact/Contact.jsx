import React from "react";
import "../Contact/Contact.css";

// The footer now contains the copyright / bottom-bar content.
// This component is kept as a no-op so existing imports don't break.
const Contact = () => null;

export default Contact;
